[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --strict-markers
    --tb=short
    --cov=event_producer
    --cov=flink_processor
    --cov=recommendation_api
    --cov-report=html
    --cov-report=term-missing